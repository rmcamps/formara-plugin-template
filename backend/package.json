{
  "name": "@formara/plugin-PLUGIN_NAME-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend for PLUGIN_NAME plugin",
  "scripts": {
    "dev": "ts-node --transpileOnly dev-server.ts",
    "build": "tsc",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:seed": "ts-node ../prisma/seed.ts",
    "setup:core": "bash ../scripts/setup-core.sh"
  },
  "dependencies": {
    "@prisma/client": "^6.8.2",
    "axios": "^1.9.0",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.18",
    "@types/express": "^5.0.2",
    "@types/node": "^24.5.2",
    "prisma": "^6.8.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}

