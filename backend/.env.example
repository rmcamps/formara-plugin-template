# Backend Standalone Configuration

# Puerto del servidor
PORT=4001

# URL del frontend del plugin
FRONTEND_URL=http://localhost:5182

# URL del core de Formara (para llamadas API)
CORE_API_URL=http://localhost:3000

# Base de datos
# Opción A: SQLite (por defecto, más rápido)
DATABASE_URL=file:../prisma/dev.db

# Opción B: PostgreSQL propio (docker-compose up)
# DATABASE_URL=postgresql://dev:dev@localhost:5433/PLUGIN_NAME_dev

# Opción C: PostgreSQL del core (datos reales)
# DATABASE_URL=postgresql://user:pass@localhost:5432/formara_dev

# Environment
NODE_ENV=development

