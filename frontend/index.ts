/**
 * Export principal del plugin
 */

import MiConfig from './components/MiConfig';

/**
 * Componentes de configuración
 */
export const components = {
  MiConfig
};

/**
 * Field types custom (si tu plugin los define)
 */
export const fieldTypes = [];

/**
 * Metadata del plugin
 */
export const pluginInfo = {
  name: 'mi-plugin',
  version: '1.0.0',
  displayName: 'Mi Plugin'
};


